# --- Core / environment ---
python-dotenv==1.0.1

# --- AWS ---
boto3==1.34.144
s3fs==2024.6.1

# --- Spark (use a PySpark version matching your JDK11 setup; 3.5.x is JDK11-friendly) ---
pyspark==3.5.1

# --- Data ingestion ---
yfinance==0.2.40
requests==2.32.3

# --- Data wrangling ---
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0        # parquet read/write outside Spark, if needed

# --- ML ---
scikit-learn==1.5.1
xgboost==2.1.0
lightgbm==4.4.0
joblib==1.4.2

# --- Sentiment / NLP ---
vaderSentiment==3.3.2
transformers==4.42.4    # only if using FinBERT; heavier install, drop if not needed
torch==2.3.1            # required by transformers/FinBERT; drop if not needed

# --- RAG / embeddings ---
sentence-transformers==3.0.1
faiss-cpu==1.8.0        # or chromadb==0.5.4 if you prefer Chroma instead of FAISS

# --- GenAI ---
groq==0.9.0

# --- Dashboard ---
streamlit==1.37.0
plotly==5.22.0

# --- Dev / notebooks ---
jupyter==1.0.0
ipykernel==6.29.5

# --- Testing ---
pytest==8.2.2
httpx==0.27.2